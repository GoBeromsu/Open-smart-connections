# Release 3.9.44

## Summary
- Reopening the same note in Connections View now reuses cached results instead of flashing loading/results again.
- Changing the embedding fingerprint now invalidates cached connection results before fresh results are rendered.
- Pending re-import and running→idle transitions now force a safe refresh path so stale connection results are less likely during note switching.
