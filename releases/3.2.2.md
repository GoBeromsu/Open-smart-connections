## patch `v3.2.2`

- Removed chat feature and related runtime-unreachable legacy code paths.
- Removed unused `assets` bundle and legacy chat/context type surfaces.
- Consolidated `core` into a feature-first `src/app`, `src/features`, `src/shared` layout.
- Stabilized CI bundling for pnpm symlinked dependencies and simplified duplicate CI trigger behavior.
