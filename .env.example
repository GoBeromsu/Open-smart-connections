# Vault selection for `pnpm dev`
# VAULT_PATH=/path/to/your/vault
# VAULT_NAME=MyVault

# For Smart Connections build: auto-copy to these vaults (comma-separated).
# Prefer absolute paths. Relative paths resolve from repo root (and, for back-compat, parent dir).
# NOTE: `pnpm dev` sets DESTINATION_VAULTS automatically from your vault selection.
# DESTINATION_VAULTS=/absolute/path/to/vault,/another/vault

# GitHub token for releases (create at https://github.com/settings/tokens)
# Required scopes: repo (for private repos) or public_repo (for public repos)
GH_TOKEN=your_github_token_here

# GitHub repository in format: owner/repo
GH_REPO=your_username/obsidian-smart-connections

# OpenRouter API key (embedded in builds)
# DEFAULT_OPEN_ROUTER_API_KEY=
